{
  "name": "pehli-kamai-admin-api",
  "private": true,
  "description": "Backs the /admin grievance dashboard's two serverless functions (docs/api). The rest of the site is plain static HTML/CSS/JS and needs no build step -- this file exists only so Vercel installs firebase-admin for those two functions.",
  "dependencies": {
    "firebase-admin": "^12.7.0"
  }
}
